{
    "name": "plate-saver",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend/built && node backend-server.js",
        "prebuild": "cd backend && npm run build",
        "build": "cd Frontend && npm run build"
    }
}